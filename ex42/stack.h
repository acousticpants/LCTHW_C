#import "dbg.h"
//maybe see ex16 structs
typedef Struct stack{//keeps track of length and current position with cur
    int length[];
    int cur;
} stack;

stack *Stack_create(length);
Stack_push();
Stack_pop();
Stack_peek();
Stack_destroy();